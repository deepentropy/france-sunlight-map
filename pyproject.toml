[project]
name = "sunlight-extractor"
version = "0.1.0"
description = ""
requires-python = ">=3.11"
dependencies = [
    "cupy-cuda12x",
    "rasterio",
    "py7zr",
    "numpy",
    "pvlib",
    "matplotlib",
    "pyproj",
    "requests",
    "tqdm",
]

[tool.uv]
package = false
